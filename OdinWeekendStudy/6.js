let shoppingDone = false;
let childsAllowance;

// We don't need to explicitly specify 'shoppingDone === true' - you can just write it short-hand so that whatever is in the brackets evaluates to true
if (shoppingDone) {
  childsAllowance = 10;
} else {
  childsAllowance = 5;
}