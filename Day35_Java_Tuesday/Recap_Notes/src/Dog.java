
public class Dog {
    // Fields - this is info about your object (Animal in this case)
    //You need to set out the accessibility of your fields, what datatype your fields contain and the name of the field
    private String name;
    private int age;

    //Constructor
    //This is the way that you create animals - so this is like a POST (but only doing it to the local variable)
    public Dog(String name, int age){
        this.name = name;
        this.age = age;
    }

    //Getters and Setters
    //This is what allows you to access the fields and 
}
